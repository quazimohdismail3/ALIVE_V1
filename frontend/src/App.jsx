// Phase 3 placeholder — real state machine + screens land in Phase 4.
// This file exists so `npm run dev` and `vite build` work during Phase 3.
import React from 'react'

export default function App() {
  return (
    <div className="app" data-state="ventral_vagal">
      <div className="screen">
        <h1 className="display" style={{ fontSize: 32, letterSpacing: '0.12em' }}>MISSION ALIVE</h1>
        <p className="secondary">Phase 3 foundation loaded. Screens arrive in Phase 4.</p>
      </div>
    </div>
  )
}
